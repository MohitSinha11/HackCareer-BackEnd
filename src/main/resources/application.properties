spring.application.name=backend
# POSTGRES
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hackcareer}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:changeme!}

# JPA
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true

# SERVER
server.port=${PORT:8080}

# APP SECURITY
# Base64-encoded secret key for JWT signing (keep this secure in production)
app.jwt.secret=${APP_JWT_SECRET:VUZwVjBTZnNwWEZjUnY0N2V4eEgrRUVjSXByS2R4VW95YWl3S2VQMFV4dkhLdkN2STVhQ1VjQnRmQnZ0QmZudw==}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:https://*.vercel.app}

# BOOTSTRAP ADMIN
app.bootstrap.admin.email=${APP_BOOTSTRAP_ADMIN_EMAIL:admin@hackcareer.com}
app.bootstrap.admin.password=${APP_BOOTSTRAP_ADMIN_PASSWORD:Admin@123}
app.bootstrap.admin.name=${APP_BOOTSTRAP_ADMIN_NAME:HackCareer Admin}
